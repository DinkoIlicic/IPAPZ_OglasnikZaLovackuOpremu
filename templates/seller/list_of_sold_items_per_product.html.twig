{% extends '/seller/header.html.twig' %}

{% block body %}
    <div>
        <a class="btn btn-primary" href="{{ path('seller_index') }}">Back</a>
    </div><br />
    <div>
        {{ form_start(form) }}
            {{ form_row(form.product) }}
        <button type="submit" class="btn btn-primary" formnovalidate>Show</button>
        {{ form_end(form) }}
        <br/><br />
        <p><a class="btn btn-primary" href="{{ path('list_of_sold_items_per_product') }}">Show all</a></p>
    </div><hr />
    <h4>{{ message }}</h4>
    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th scope="col">Product Name</th>
                <th scope="col">Buyer</th>
                <th scope="col">Quantity</th>
                <th scope="col">Price</th>
                <th scope="col">Total Price</th>
                <th scope="col">Bought at</th>
                <th scope="col">Status</th>
            </tr>
        </thead>
        <tbody>
            {% for eachItem in soldItems %}
                <tr>
                    <td><a href="{{ path('view_sold_product_info', {'id': eachItem.id}) }}" class="card-link">{{ eachItem.product.name }}</a></td>
                    <td>{{ eachItem.user.fullName }}</td>
                    <td>{{ eachItem.quantity }}</td>
                    <td>{{ eachItem.price }}</td>
                    <td>{{ eachItem.totalPrice }}</td>
                    <td>{{ eachItem.boughtAt|date("d.m.Y H:i") }}</td>
                    <td>{% if(eachItem.confirmed == 0) %}
                            <a class="btn btn-primary" href="{{ path('confirm_buy_per_product', {'id': eachItem.id}) }}">Confirm</a>
                        {% else %}
                            <a class="btn btn-primary" href="{{ path('confirm_buy_per_product', {'id': eachItem.id}) }}">Confirmed</a>
                        {% endif %}
                    </td>
                    {% if(eachItem.confirmed == 0) %}
                        <td>
                            <a class="btn btn-danger" href="{{ path('delete_sold_item_per_product', {'id': eachItem.id}) }}">Delete</a>
                        </td>
                    {% endif %}
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}