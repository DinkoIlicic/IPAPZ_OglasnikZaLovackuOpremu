{% extends 'base.html.twig' %}

{% block body %}
    <div>
        <p>Update my product image</p>
    </div>
    <div>
        {{ form_start(form) }}
            {{ form_row(form.image) }}
            <button type="submit" class="btn btn-primary" formnovalidate>Insert</button>
        {{ form_end(form) }}
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}