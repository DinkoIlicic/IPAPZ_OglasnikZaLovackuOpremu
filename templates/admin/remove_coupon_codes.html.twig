{% extends '/admin/header.html.twig' %}
{% block body %}
    <div>
        <a class="btn btn-primary" href="{{ path('admin_index') }}">Back</a>
    </div><br />
    <div>
        <p>Remove coupon codes</p>
    </div>
    <div>
        {{ form_start(form) }}
            {{ form_row(form.startId) }}
            {{ form_row(form.endId) }}
            <button type="submit" class="btn btn-primary" formnovalidate>Delete</button>
        {{ form_end(form) }}
    </div>
{% endblock %}