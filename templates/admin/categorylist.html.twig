{% extends 'base.html.twig' %}

{% block body %}
    {{ message }}

    <table class="table">
        <thead>
        <tr>
            <th scope="col">Category Name</th>
            <th scope="col">User</th>
            <th scope="col">Visibility</th>
        </tr>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr>
                <td><a href="{{ path('checkonecategory', {'id': category.id}) }}" class="card-link">{{ category.name }}</a></td>
                <td>{{ category.user.firstname }} {{ category.user.lastname }}</td>
                <td>{{ category.visibility }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <br />
    <hr />

    <p>New Category</p>
    {{ form_start(form) }}
        {{ form_row(form.name) }}
        <button type="submit" class="btn btn-primary" formnovalidate>Insert</button>
    {{ form_end(form) }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}

{% endblock %}