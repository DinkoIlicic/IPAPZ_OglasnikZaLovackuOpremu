<!DOCTYPE html>
<html lang="hr">
<head>
    <title>Invoice</title>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
</head>
{% block body %}
    <h2>Invoice</h2>
    <h4>User:</h4>
    <p>{{ sold.user.fullName }}</p><br>
    <h4>Product:</h4>
    <p>Name: {{ sold.product.name }}</p>
    <p>Price: {{ sold.price }}</p>
    <p>Quantity: {{ sold.quantity }}</p>
    <p>Total Price: {{ sold.totalPrice }}</p>
    <p>Discount: {{ sold.discount }}</p>
    <p>After Discount: {{ sold.afterDiscount }}</p>
    <p>Shipping: {{ sold.shippingPrice }}</p>
    <p>To pay: {{ sold.toPay }}</p>
{% endblock %}